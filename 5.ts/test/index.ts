function hello(name:string){
    console.log(`hello${name}`)
}

hello("Typescript")

//문자열

let someValue:any;
//이 변수에 대한 타입체크를 하지 않음

someValue.toSTring();
someValue =false;
someValue.toFixed();

//여러가지 변수의 타입 사용 가능. 재사용도 가능 ->안정성 떨어짐 사용안하는게 좋음

